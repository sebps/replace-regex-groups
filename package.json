{
  "name": "replace-regex-groups",
  "version": "1.0.3",
  "description": "A lightweight library to replace the regex groups in a string by a set of replacement values",
  "main": "lib/index.js",
  "scripts": {
    "test": "mocha"
  },
  "author": "sebps",
  "homepage": "https://github.com/sebps/replace-regex-groups",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^8.2.1"
  }
}
